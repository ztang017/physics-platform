{
  "name": "physicslab-feedback-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute physicslab-feedback --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute physicslab-feedback --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "~5.7.2",
    "wrangler": "^4.0.0"
  }
}
